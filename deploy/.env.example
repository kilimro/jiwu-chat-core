# ============================================================
# JiwuChat 发布包配置示例（复制为 .env 后按需修改）
# 使用：./start.sh 或 docker compose up -d  详见 CONFIG.md
# ============================================================

# ---------- 必填 ----------
JIWU_CHAT_IMAGE=ghcr.io/kiwi233333/jiwu-chat-core:latest

# ---------- Docker 与端口（默认如下，可按需修改）----------
DOCKER_REGISTRY=docker.1ms.run
WEB_PORT=3000
API_HTTP_PORT=9090
API_WS_PORT=9091

# ---------- 前端（部署时生效，重启容器即可）----------
VITE_API_BASE_URL=http://localhost:9090/
VITE_API_WS_BASE_URL=ws://localhost:9091/
# VITE_BASE_OSS_PATH=

# ---------- 中间件默认账号（与官方镜像一致）----------
MYSQL_ROOT_PASSWORD=password
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# ---------- 后端：邮件（可选）----------
# MAIL_HOST=
# MAIL_PORT=25
# MAIL_USERNAME=
# MAIL_PASSWORD=

# ---------- 后端：七牛云（可选）----------
# QINIU_ACCESS_KEY=
# QINIU_SECRET_KEY=
# QINIU_BUCKET=
# QINIU_HOST=
# QINIU_SIGN_KEY=
# QINIU_SIGN_KEY2=

# ---------- 后端：短信 UniSMS（可选）----------
# UNISMS_ACCESS_KEY=
# UNISMS_SECRET_KEY=
# UNISMS_TEMPLATE_ID=
# UNISMS_SIGNATURE=
# UNI_SMS_WEBHOOK_SIGNATURE_SECRET=

# ---------- 后端：腾讯翻译（可选）----------
# TENCENT_SECRET_ID=
# TENCENT_SECRET_KEY=

# ---------- 后端：AI 翻译 / MCP（可选）----------
# RES_TRANSLATION_API_URL=https://api.siliconflow.cn/v1
# RES_TRANSLATION_API_KEY=
# RES_TRANSLATION_MODEL=Qwen/Qwen2.5-32B-Instruct
# RES_TRANSLATION_MAX_TOKENS=4096
# MCP_ALLOWED_KEY=
